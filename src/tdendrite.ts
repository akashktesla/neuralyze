export default class tdendrite{
  id:number;
  output:number;
  weight:number;

  constructor(id:number,output:number,weight:number){
    this.id = id;
    this.output = output;
    this.weight = weight;
  }
}
