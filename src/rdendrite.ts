export default class tdendrite{
  id:number;
  input:number;
  weight:number;

  constructor(id:number,input:number,weight:number){
    this.id = id;
    this.input = input;
    this.weight = weight;
  }
}
